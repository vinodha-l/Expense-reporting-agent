[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "expense-reporting-agent"
version = "0.1.0"
description = "Expense auditing environment"
dependencies = [
    "openenv-core>=0.2.0",
]

[project.scripts]
server = "server.app:main"
