[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "expense-reporting-agent"
version = "0.1.0"
description = "Real-world expense auditing environment"
dependencies = [
    "pydantic",
]
